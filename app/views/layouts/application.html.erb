<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>WebWeveloperCV.com</title>
  <%= include_stylesheets :common %>
  <%= include_javascripts :common %>
  <%= yield :head %>
  <%= csrf_meta_tag %>
  <% if Rails.env != 'production1' %>
  <script>
    var disqus_developer = 1;
  </script>
  <% end %>
</head>
<body>
  <div id="<%= @random_str %>" data-role="page"<%= (defined? @page_class).nil? ? "" : " class='#{@page_class}'" %><%= (defined? @page_attr).nil? ? "" : " #{@page_attr}" %>> 
    <header class="wdcv-header clearfix">
      <hgroup>
        <h1 class="wdcv-site-logo top bottom"><a href="/">WebDeveloperCV.com</a></h1>
        <h2 class="wdcv-site-description top bottom small"><a href="/">Evgeny Neumerzhitskiy's personal bits</a></h2>
      </hgroup>      
    </header> 
    <div class="wdcv-content">
      <% unless notice.nil? %>
        <div class="notice hidden"><%= notice %></div>
      <% end %>
      <% unless alert.nil? %>
        <div class="alert hidden"><%= alert %></div>
      <% end %>    
      <%= yield %>
    </div>
    <% if user_signed_in? %>
      <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
    <% end %>
  </div>
</body>
</html>
